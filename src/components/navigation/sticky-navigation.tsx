import './sticky-navigation.scss';

import React from 'react';
import Navigation from './navigation';

const StickyNavigation = () => (
    <div className="sticky-navigation">
        <Navigation />
    </div>
);

export default StickyNavigation;
